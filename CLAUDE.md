Project A is reference (read-only)

Project B is target

Must preserve in Project B:

API client files

endpoints, request/response types

auth/session logic

env var names

Allowed changes:

components, pages, layout, styling, UI state wiring

Output style:

match A’s design system (spacing, typography, colors, components)